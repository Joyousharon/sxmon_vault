本教程通过宝塔面板安装搭建，所以确保你已经安装好了宝塔面板。
### 📋 0. 环境准备

在宝塔面板「软件商店」中安装以下软件，**版本必须对应**，否则容易报错：

- **MySQL**: 5.7 (高版本可能存在兼容性问题)
    
- **Nginx**: 1.25
    
- **Java环境**: JDK 1.8.0 (即 JDK 8，**必须安装**)
    

---

### 📂 1. 文件部署与软连接

此步骤是为了确保程序能读取到正确路径下的静态资源。

1. **下载文件**：[https://hssq.lanzouw.com/iwQuy2x4hu4b](https://www.sakuraidc.cc/?golink=aHR0cHM6Ly9oc3NxLmxhbnpvdXcuY29tL2l3UXV5Mng0aHU0Yg==&nonce=82f4d7a45d)
    
2. **上传解压**：
    
    - 打开宝塔 -> **【文件】**。
        
    - 进入目录：`/www/wwwroot`。
        
    - 上传压缩包并解压。
        
    - **重命名**：确保解压后的文件夹名称为 `poetize`。
        
    - 此时你的物理路径应为：`/www/wwwroot/poetize`。
        ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209230729547.png)
1. **创建软连接（关键）**：
    
    - 在文件管理器中进入 `/home` 目录。
        
    - 点击 **【终端】** 或者在宝塔的 **【新建 -> 创建软连接】**。
    - ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209230803088.png)
        
    - **方法A（推荐命令行，更准确）**：
        
        打开终端，执行以下命令：
        
    - **检查**：进入 `/home` 目录，看到有一个名为 `poetize` 的浅蓝色文件夹（链接文件）即成功。
        ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209230847540.png)

---

### ⚙️ 2. 修改后端配置

1. 进入目录：`/www/wwwroot/poetize`。
      
2. 找到配置文件（ppoetry.spl）。
    ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209231052404.png)
3. **替换 IP 为域名**：
    
    - 点击编辑，按 `Ctrl+F` 搜索原始 IP：`192.168.6.132`。
        
    - 将搜索到的 IP **全部替换**为你实际要访问的域名。
        
    - 保存文件。
        

---

### 🚀 3. 运行服务端 (Java后端)

1. **创建数据库**：
    
    - 进入宝塔 **【数据库】**，添加数据库 `poetize` (如果压缩包内有 `.sql` 文件，请务必先导入数据库；如果没有，程序可能会自动生成)。
        
2. **添加 Java 项目**：
    
    - 点击 **【网站】** -> **【Java项目】** -> **【添加Java项目】**。
        
    - **项目类型**：Spring Boot。
        
    - **Jar路径**：选择 `/www/wwwroot/poetize` 里面的 jar 包。
        
    - **端口**：`8081`。
        
3. **配置启动参数（核心步骤）**：
    
    - 在“项目执行命令”的末尾（注意先打一个**空格**），粘贴以下代码：
    - 
        --spring.datasource.username=root --spring.datasource.password=密码
        
    - _请将 `你的数据库密码` 替换为你宝塔数据库 root 的真实密码。_
        ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209231337766.png)
1. **选项勾选**：
    
    - [x] 守护进程
        
    - [x] 监听重启
        
2. 点击确定启动。查看日志，显示 `Started` 即为启动成功。
    

---

### 🌐 4. Nginx 反向代理配置（完整代码）

这是你最关心的部分。请按照以下步骤操作，代码直接复制即可。

1. **添加站点**：
    
    - **【网站】** -> **【PHP项目】** -> **【添加站点】**。
        
    - 域名：填写你的域名。
        
    - PHP版本：纯静态。
        ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209231457708.png)
1. **配置 SSL**：
    
    - 在站点设置中，点击 **【SSL】**，申请 Let's Encrypt 证书并部署。
        
    - 右上角开启 **【强制HTTPS】**。
        
2. **修改配置文件**：
    
    - 点击站点设置 -> **【配置文件】**。
        
    - **全选清空**当前所有内容。
        
    - **复制下方完整代码**粘贴进去。
    -
	server {
    listen                          443 ssl;
    server_name                     192.168.6.132; #域名
    ssl_certificate                 /www/server/panel/vhost/cert/192.168.6.132/fullchain.pem;
    ssl_certificate_key             /www/server/panel/vhost/cert/192.168.6.132/privkey.pem;
    ssl_session_timeout              5m;
    ssl_protocols                    TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers                      ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
    ssl_prefer_server_ciphers  on;

    # 禁止访问隐藏目录(.git/)和隐藏文件(.file)和遍历目录(../)
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    location / {
        root       /home/poetize/poetize-ui/; #前端路径，注意域名
        index      index.html;
        try_files  $uri $uri/ /index.html;
    }

    location /im {
        alias      /home/poetize/poetize-im-ui/; #这里聊天室路径，注意域名
        index      index.html;
        try_files  $uri $uri/ /index.html;
    }

    location /api/ {
        rewrite           ^/api/(.*)$ /$1 break;
        proxy_pass        http://127.0.0.1:8081;
        proxy_redirect    off;
        proxy_set_header  Host $host;
        proxy_set_header  X-real-ip $remote_addr;
        proxy_set_header  X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /socket {
        proxy_pass          http://127.0.0.1:9324;
        proxy_http_version  1.1;
        proxy_set_header    Upgrade $http_upgrade;
        proxy_set_header    Connection "upgrade";
        proxy_read_timeout  600s;
    }


    #静态文件存储的目录（这里是本地存储的路径设置，需要和源码哪里的路径一模一样，注意域名的不同）
    location /static/ {
        alias /home/poetize/file/; 
        autoindex off;
        valid_referers 192.168.6.132;
        if ($invalid_referer) {
            #return 403;
        }
    }
}

3.**替换工作**：按 `Ctrl+F` 搜索 `192.168.6.132`，全部替换为你的 **域名**。
    ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209231810327.png)

#### 📄 Nginx 完整配置代码

4. 保存配置。
    

---

### ✅ 5. 验证与收尾

1. **访问**：在浏览器输入你的域名。
    ![](assets/🌸%20Poetize（最美博客）最新保姆级部署教程/file-20260209231848918.png)
2. **后台登录**：
    
    - 入口：通常在博客底部的“登录”按钮或域名后加 `/admin`。
        
    - **账号**：`admin`
        
    - **密码**：`admin`
        
3. **安全建议**：
    
    - 登录成功后，请立即配置邮箱（如果有配置项）并修改默认密码。
        
    - 如果你开启了防火墙，确保宝塔面板放行了 `8081` (后端) 和 `9324` (IM聊天) 端口（虽然走了反代通常不需要对外开放，但如果是多机部署则需要）。
        

